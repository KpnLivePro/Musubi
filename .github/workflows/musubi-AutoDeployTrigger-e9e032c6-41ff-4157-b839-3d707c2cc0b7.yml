name: Trigger auto deployment for musubi

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/musubi-AutoDeployTrigger-e9e032c6-41ff-4157-b839-3d707c2cc0b7.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.MUSUBI_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.MUSUBI_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.MUSUBI_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: kpnworldcloud.azurecr.io
          registryUsername: ${{ secrets.MUSUBI_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.MUSUBI_REGISTRY_PASSWORD }}
          containerAppName: musubi
          resourceGroup: jeremiah-rg
          imageToBuild: kpnworldcloud.azurecr.io/musubi:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


